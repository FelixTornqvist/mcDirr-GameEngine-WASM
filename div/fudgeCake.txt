Sprite() {



  void virtual draw() {
      renderCopy(bild {this.x , this.y, this.width ....});
      //bara utritning
  }
    
  void setHit(bool isHit) {
    this.hit = hit;
    this.x -= 10;
  }



  void virural tick(int milliseconds) {
    if(keys.isPressed(KEY_W)) {
      this.x += this.speed/*pixels per milliseconds*/ * milliseconds; //t.ex.
    }
  }  
}

https://wiki.libsdl.org/SDL_IntersectRect

